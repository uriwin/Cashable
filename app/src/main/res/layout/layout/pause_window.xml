<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/darker_gray">


    <Button
        android:id="@+id/restart_against_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Restart"
        android:layout_marginBottom="35dp"
        android:layout_above="@+id/resume"
        android:layout_alignStart="@+id/resume"
        android:layout_alignEnd="@+id/resume" />

    <Button
        android:id="@+id/resume"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Resume"
        android:layout_above="@+id/back_to_menu"
        android:layout_marginBottom="35dp"
        android:layout_alignEnd="@+id/back_to_menu"
        android:layout_alignStart="@+id/back_to_menu" />

    <Button
        android:id="@+id/back_to_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="back to menu"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/music"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Music"
        android:layout_marginTop="35dp"
        android:layout_below="@+id/back_to_menu"
        android:layout_alignStart="@+id/back_to_menu"
        android:layout_alignEnd="@+id/back_to_menu" />

</RelativeLayout>